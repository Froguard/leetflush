/**
 * 希尔排序（优化版的插入排序）
 *
 * @param {Array<number>} nums
 * @param {Boolean} isDesc 是否为降序，默认位升序，即false
 * @returns {Array<number>} nums
 */
function shellSort(nums, isDesc) {
  // TODO: 其为简单插入排序的进阶版，优先级不高，先放着
}
